package com.gameshop.model.enums;

/**
 * Enum representing different types of time periods for dashboard statistics
 */
public enum PeriodType {
    MONTH, // Monthly statistics
    QUARTER, // Quarterly statistics
    YEAR // Yearly statistics
}
